PORT=3000
NODE_ENV=production
HOST=0.0.0.0

# Database Configuration (Replace with your actual Dokploy PostgreSQL credentials)
# Format: postgresql://<user>:<password>@<db-container-name>:<db-port>/<db-name>
DATABASE_URL=postgresql://postgres:Jackson%402024@digitssales-database-br4rx7:5432/digitsales

# Security
JWT_SECRET=digisales-pos-production-super-secret-key-9f8d7e6c5b4a
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (Allows your secure frontend to talk to this backend)
CORS_ORIGIN=https://books.digits.co.tz

# API Versioning defaults
API_VERSION=v1
API_PREFIX=/api/v1
